# Opus 视觉看板

## 当前基线

- js/battle-view.js、js/module-art.js、js/camp-ui.js 是视觉 / 界面层；本次拆分只搬移原有画面行为。
- 四足和双足视觉以最新 origin/main 为准。提交前先 pull，保留远端四足整件、首尾相连、接地点和倒车步态改动。
- 当前视觉构建标记在 js/build-vis.js：2026-09-26 side-replay-share-ui。

## 后续修改边界

- 战斗画面消费 SA.Battle.emit 的 part、text、boom、ricochet、shatter、surrender 事件。
- 规则、数值、AI、存档和分享码通过 SA.Camp、SA.S、SA.V 等接口取得；需要规则变更时写入 docs/board-opus.md 的交接记录。
- 可以直接修改 battle-view.js、module-art.js、camp-ui.js 以及 Opus 负责的精灵、腿和 CSS 文件；不要把视觉字段或四足远端基线合并回后台文件。

## 2026-09-26 本轮接口更新

- `1143948`：`battle-view.js` 的泄压、撤退和投降按钮改为调用 `SA.Battle` 操作接口；粒子 / 飘字更新由 `battle-view.tick()` 消费，战斗事件类型列表见 `js/battle.js` 顶部注释。
- `1143948`：模块外观辅助函数集中在 `module-art.js`；试驾场材料替换调用 `SA.Camp.prepareTrialVehicle()`，视觉层无需直接写车格耐久。

## 从原协作核心文档迁移的交接记录

按发起者归档；用户请求按接收者归档。记录内容与原状态原样保留，历史状态不代表当前已重新验收。跨角色请求由发起者维护，对方通过此记录读取。只追加新行，不删别人的行；完成后填写状态与提交号。

| 日期 | 从 → 到 | 内容 | 状态 |
|---|---|---|---|
| 2026-09-26 | 用户 → Opus | `docs/campaign-direction.md` §10 的 W1～W6（W1～W5 等 astra 对应项） | W1、W2 完成（`95a5111`、`8edbc62`）；W3～W5 等 astra K6 / K7 / K5+E；W6 待做 |
| 2026-09-26 | Claude → astra | **请知悉**，改动了 astra 区域：`js/vehicle.js` 分享码解码改多轮摆放（修丢模块）并新增 `SA.V.fromCells`；`tools/evolve.js` 候选记录加 `cells` / `toxicCells` / `oddCells`、`impact()` 优先用 `cells`、P7 夹具扰动改为 0.1；`tools/evolve-storage.js` 候选车库带 `cells`；`js/camp.js` 试驾场新增"进化报告"来源。详见 `docs/evolve-plan.md` §15 | 完成 |
| 2026-09-25 | Opus → astra | 四足整件的数据和规则：`quad` 改成 w 4 × h 2、每车一个、一台车只能用一种底盘；悬挂接地点按新画法（近后 0、近前 96、远后 8、远前 104，模块内 x），战斗里步态按「走过的距离 ÷ 4 × 步幅」推进（`SA.LEGLAB.strideFor`）。落地后 Opus 接画面并删除旧的逐格腿画法 | 完成（`ef5cd75`） |
| 2026-09-25 | 用户 → Opus | 战后一键评价按钮（好玩 / 无聊 / 不公平），数据由 astra 记录（`docs/evolve-plan.md` §11） | 完成（`10785b5`） |
| 2026-09-26 | Opus → astra | **请知悉**，动了 `js/battle.js` 的结算区域（只做最小改动）：① `projectileDamage` 里弹开时的飘字和火花换成调用 `ricochetFx`（画面用 `Math.random`，**弹开不再消耗战斗随机流**，同种子的无画面模拟结果会有细微变化）；② 弹开概率公式原样抽成 `ricochetChance(armor, weapon)` 并导出为 `SA.Battle.ricochetChance`，车间的穿深对照表用它——以后改公式请只改这一处 | 完成（`95a5111`、`fbd2606`） |
| 2026-09-26 | Opus → astra | 蓄压罐释放上限已提取到 `SA.K.BATTLE.STORE_RELEASE_PER_SEC`，战斗、HUD、编辑器和模块说明统一读取 | 已完成 |
| 2026-09-26 | Opus → 用户 | 发现并修好：战斗飘字只有数字和几个字母的像素字，中文（"弹开""投降"）**从来没显示过**；现在中文飘字画成叠加层的小铭牌 | 完成（`95a5111`） |
| 2026-09-26 | Opus → astra | **请知悉**：`js/battle.js` 腿式步态改成步态角 `s.gait += 2π × 距离 ÷ (4 × 步幅)`（原来写成了 距离 ÷ 4 × 步幅，是交接时写得含糊；和 `tools/chassis-lab.html` 一致，只影响画面）；绘制参数加了 `speed` | 完成（见本次 `vis:` 提交） |
| 2026-09-26 | Opus → astra | **双足占格和 `docs/true-biped.md` §8 对不上**：§8 定的是双足占「最下面两**层**」（大格：胯一层 + 腿区一层 = 4 个子格行，样机 `bipedArt` 是 2×4 子格、48×96），现在的数据是 `w: 1, h: 2`（子格，胯和腿区各只有 1 个子格行、24 宽），腰挂位也按子格算。请按 §8 改成层（或和用户确认后改 §8），Opus 再接真双足画面 | 用户已定（§5），改动见下一行 |
| 2026-09-26 | Opus → astra | **请知悉**（用户授权越权修改）：四足多件相连——`modules.js` 的 `quad` 去掉 `chassisLimit: 1`、加 `chain: true`；`vehicle.js` 的 `normalizeChassis`（旧的逐格四足从左往右只留不重叠的）、`canPlace`（新四足要和已有的首尾相连）、`issues`（只认最长一段相连的，其余标「隔着空子」）；`tools/evolve-check.js` 加了相连 / 隔空两个夹具 | 完成（见本次 `sys:` 提交） |
| 2026-09-26 | Opus → astra | **请知悉**：四足接地点跟着新腿形改成 近后 18 / 近前 78 / 远后 20 / 远前 80（`contactPts`，视觉字段，`evolve-check` 同步改了）；`battle.js` 的步态角改成按车头方向带符号（倒车时步态倒放），四足用自己的步幅 `LEGLAB.quadStride` | 完成（见本次 `vis:` 提交） |

| 2026-09-26 | Opus → astra | **请知悉**（用户直接要求 Opus 修，接手了 astra 看板上「按 §5 把真双足改成 2×4 子格」那一项）：`modules.js` 的 `biped` 改成 `w: 2, h: 4`；`vehicle.js` 底盘锚点行改成 `chassisRow(id) = ROWS - h`（双足第 8 行，履带 / 四足仍是第 10 行），新增 `chassisRow` / `chassisRowOf` / `bipedOf` / `floorRow` 接口；腰挂位 = 胯层左右各 1 大格；双足撞击件只能在胯层；`normalizeChassis` 里旧双足（第 10 行的逐格或 1×2）整车上移一层、胯放在车身重心下、躯干上的撞击件挪到胯前；分享码解码同样处理；平衡 d / 重心高度改成按大格算（§2 的阈值单位）；`battle.js` 悬挂取底盘所在行、胯 / 腿分区按新行判；`tools/evolve.js` 底盘放在 `chassisRow`；`evolve-check` 夹具换成 2×4、加「只能一个双足」。`tools/split-baseline.json` 重新生成：92 局变化全部是双足参与的局（玩家双足 68 局，敌方独角兽 / 差分机 24 局），其余 180 局不变。关卡里的双足车迁移后都平衡、无违规；差分机的一个变体有一门 mg2（`subs`）本来就悬空，请 astra 看一下。另外 `susp.follow` 0.85 让双足在坡上整体倾斜，样机里双足是基本直立、靠腿够地面，要不要调低请 astra 决定 | 完成（见本次 `sys:` / `vis:` 提交） |
| 2026-09-26 | Opus → astra | 请同步 `docs/module-plan.md`：双足改为「2×4 整件（胯层 + 腿区），真双足画面进游戏（`bipedArt`：陀螺胯、腰挂法兰、躯干切角、一对长腿），旧逐格双足画法删除」 | 待做 |
| 2026-09-26 | Opus → 用户 | 美术风格速查 `docs/art-style.md`（一句话风格、五条支柱、硬规则、调色板、材质配方、剪影母题、场景 / UI、做 / 不做、验收清单、P1～P7 生成提示词）+ 实物参考页 `tools/style-guide.html`（开发者面板「美术风格参考」，精灵和提示词都读代码 / md，不会过时）；顺手修正 `art-direction.md` 里过时的 32px 格和旧驾驶员描述。**发现**：满格的主体模块（装甲、锅炉、水箱、驾驶舱）纯黑剪影都是方块，剪影测试对它们不成立，实际靠内部大形（炉栅、水窗、拱门）+ 功能色辨认——要不要把规范改成「内部大形测试」请用户定 | 完成（见本次 `vis:` 提交） |
| 2026-09-26 | 用户 → Opus | **视觉样机收纳**：开发者面板里的 7 个视觉样机合并成一个入口「视觉样机馆」（`tools/lab.html`）：左侧按类别（美术规范 / 模块造型 / 底盘 / 地形与物理画面）收纳，每项带版本号、日期和状态（现行 / 探索中 / 已进游戏 / 历史存档），底盘演进线 v1→v4 可以直接跳；登记表 `tools/labs.js`，每个样机页单独打开时右上角有回到样机馆的导航标签。样机文件本身没挪位置，文档里的路径都还有效 | 完成（见本次 `vis:` 提交） |
| 2026-09-26 | 用户 → Opus | **小炮造型 + 材质语法探索**（`tools/cannon-s-lab.html`，样机馆「模块造型」）：五个方向（A 小炮塔 / B 舰炮座 / C 卡隆短炮 / D 炮窗 / E 喇叭口）× 六种材料；材质语法 = 每种材料提供自己的零件画法（面板、炮管截面、箍、炮口、耳轴、紧固件、动态），换材料是换做工而不是换色，并和现在的装饰层并排对照、放进车体对照。建议 C 为主、A 备选；待用户定：造型方向、T1「黄铜」是否真画成黄铜、以太炮口闪光颜色、材质语法是否推广 | 探索中，等用户定 |
| 2026-09-26 | Opus → astra | **请知悉**：`js/sprites.js` 对外多导出了 `decorate`（材料装饰层，只给样机页对照用，游戏行为不变）；`js/camp-ui.js` 的 `DEV_TOOLS` 7 个视觉样机合并成「视觉样机馆」一项 | 完成 |

| 2026-09-26 | 用户 → Opus | 给已完成的后台功能接界面：W3 支线入口、W4 重打入口、唯一件显示、分享码车库整理；战后一键评价按钮 | 完成（`10785b5`，用户代提交；本次补看板）：① W3：出战页「遭遇战」页签有新遭遇角标，行上 ★ 标出可缴获的唯一件，对阵面板有「竞技场外：没有观众、没有奖金和声望」条和战损说明、奖励卡；战斗背景把看台换成工厂砖墙（没有观众），计时器标「竞技场外」② W4：战役列表按章分组折叠（当前章展开，折叠条显示已通关 / 进行中 / 未开放和进度），重打行 ↺，对阵面板有重打说明 ③ 唯一件：金色「★ 唯一件」徽章（缴获弹窗金边卡片 + 只有这一次的提示、模块清单、选中面板、悬停提示、对阵面板「对手身上的唯一件」），卖掉 / 报废唯一件先确认 ④ 分享码车库：去掉「云端」字样，新增「分享当前车辆」，分享弹窗（预览 + 分享码 + 复制，剪贴板不可用时提示手动复制），导入弹窗边粘贴边预览、读不出来会说明，示例车可「存为我的」⑤ 战后结算和重打结算都有「好玩 / 无聊 / 不公平」三个按钮，调 `SA.HUMAN_BATTLES.feedback`；`battle.js` 的 `recordHumanBattle` 改成返回记录 id，结算数据带 `humanId`（最小改动） |
| 2026-09-26 | Opus → astra | **三个后台问题（未改，请处理）**：① `SA.isUnique(id)` 按模块 id 判定，支线奖励写了 `periscope`、`armor_heavy`，结果所有观察镜、重装甲都成了唯一件——商店买不到（`buyable`）、旧存档里有这两种的会被记成已领取；唯一件应该按「这一件」而不是按模块种类判定。② 两场遭遇战的奖励靠「对手车上存活的同 id 模块」掉落，但码头巡逻队车上没有观察镜、工厂护送车车上没有重装甲，奖励永远掉不出来（实测胜利后提示「对手车上没有你缺的零件」）。③ `salvageOptions` 先随机、只把材料 ≥5 的排前面再取 3 件，材料低的唯一件（熟铁观察镜）可能被挤掉；唯一件应该永远排在最前 | 待做 |

## 原工作清单

状态：待做 / 进行中 / 完成 / 搁置。以下为迁移时原文。

### Opus（视觉）

| 项 | 内容 | 状态 |
|---|---|---|
| V1 底盘画面接入 | 四足整件（一整块甲壳 + 四条腿、按距离的大步态、四脚悬挂）；真双足：胯和一对腿、躯干切角和姿态错位、跪地 / 陀螺停转的表现、编辑器平衡指示 | 四足完成；真双足 2×4 画面接入完成（胯、腰挂法兰、躯干切角、长腿、步态、悬挂）；姿态错位、跪地 / 陀螺停转、编辑器平衡指示待做 |
| V2 现有模块阶段造型 | `docs/module-plan.md` §5 阶段 1～4、8 里**现有模块**的部分 | 完成（`3e04471`）：中炮、侧炮、直射火炮、履带三阶段；锅炉、撞击件 T5；联合驾驶舱四人舱；驾驶员 T5 换装。旧逐格四足 / 双足不做，等 V1 |
| V3 通用层 | 材料装饰层 6 套、改装挂件 | 完成（`15b7520`） |
| V4 UX | 新属性的呈现（储能、省水、不耗水散热、牵引）、解锁和缴获弹窗的节奏、新手引导、移动端 | 进行中：新属性 + 穿深 / 装甲厚度呈现完成（`fbd2606`）；弹窗节奏、新手引导、移动端待做 |
| V5 地形美术 | 配合 astra D 的新地形元素 | 等 astra D |
| W 战役方向配套 | `docs/campaign-direction.md` §10：跳弹反馈、修理费呈现、支线入口、重打入口、双足唯一件画面、早期瞄准手感 | W1、W2、W3、W4 完成；W5 等 astra；W6 待做 |
| V6 新模块美术与特效 | 13 个占位模块、Boss 件、鱼叉绳索、火焰、火箭尾焰 | **搁置**（docs/collab.md §5：新模块现在只做功能） |


### 搁置

音乐和音效；云车库服务器存储；新模块的专属造型和特效。
